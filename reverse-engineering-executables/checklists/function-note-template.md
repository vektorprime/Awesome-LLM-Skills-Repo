### `<name>` at `<VA/RVA>`
**Purpose:**
**Inputs (per ABI: rcx/rdx/r8/r9 or stack):**
-
**Outputs (rax + side effects):**
-
**Observed behavior (with addresses):**
-
**Open questions:**
-
**Evidence (artifacts + xrefs + runtime hits):**
-
